{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "cd C:\\Users\\ivicd\\ogrenci-servis-app; $s = git status --porcelain 2>$null; if ($s) { git add -A; $msg = 'guncelleme: ' + (Get-Date -Format 'yyyy-MM-dd HH:mm'); git commit -m $msg 2>&1; git push origin master 2>&1 }",
            "timeout": 30,
            "statusMessage": "GitHub'a gönderiliyor..."
          }
        ]
      }
    ]
  }
}